# Project Rules

## Git Workflow
- NEVER commit directly to `main` branch
- Always create a feature branch before making changes
- Use branch naming: `feature/<description>` or `fix/<description>`
- Push to the feature branch, then create a PR
